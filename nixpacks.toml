[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.build]
cmds = ["python3 -m pip install -r requirements.txt"]

[phases.install]
cmds = []

[start]
cmd = "python3 railway_server.py"
